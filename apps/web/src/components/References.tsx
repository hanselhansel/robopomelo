import {useState} from 'react';
import type {Collection,Deployment} from '@robopomelo/spec';
import {PagedList} from './ui.js';
export interface ReferenceOption {id:string;label:string;collection:Collection}
export function referenceOptions(deployment:Deployment,targets:Collection|Collection[]):ReferenceOption[]{return (Array.isArray(targets)?targets:[targets]).flatMap(collection=>deployment[collection].map(record=>({id:record.id,label:record.title,collection}))).sort((a,b)=>a.label.toLocaleLowerCase().localeCompare(b.label.toLocaleLowerCase())||a.id.localeCompare(b.id));}
export function References({id,label,value,options,onChange,onView}:{id:string;label:string;value:string[];options:ReferenceOption[];onChange:(ids:string[])=>void;onView?:(id:string)=>void}){const[open,setOpen]=useState(false);return <fieldset id={id}><legend>{label}</legend>{value.length===0?<p className="help">No linked records yet.</p>:<ul className="selected-references">{value.map(target=><li key={target}><span>{options.find(o=>o.id===target)?.label??'Unavailable record'} <small>({target})</small></span>{onView&&<button type="button" onClick={()=>onView(target)}>View</button>}<button type="button" aria-label={`Unlink ${options.find(o=>o.id===target)?.label??target}`} onClick={()=>onChange(value.filter(v=>v!==target))}>Unlink</button></li>)}</ul>}<button type="button" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?'Close record choices':`Choose ${label.toLowerCase()}`}</button>{open&&<PagedList items={options} label="record choices" searchText={o=>`${o.label} ${o.id} ${o.collection}`}>{o=><label className="check-row" key={o.id}><input type="checkbox" checked={value.includes(o.id)} onChange={e=>onChange(e.target.checked?[...value,o.id]:value.filter(v=>v!==o.id))}/><span>{o.label}<small>{o.collection} · {o.id}</small></span></label>}</PagedList>}</fieldset>;}
